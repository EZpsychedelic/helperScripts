#include <stdio.h>

int main(int argc, char argv *[]) {
	int i;
	
	for(i = 0; i < argc; i++;)
		
	//needed calls (pg 83):
	var = open(logfile, O_WRONLY|O_CREATE|O_APPEND, S_ISUSR|S_IWUSR); //Opens/Creates and appends log file
	write(
		printf("[+] %s\n"; argv[i]);
	return(0);
}
